<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.talented.buttie.catalog.mapper.PolicyMapper">

  <!-- ========================================================= -->
  <!-- 1. 기본 조회 (단건, 전체, 페이징)                         -->
  <!-- ========================================================= -->
  <select id="findById" resultType="com.talented.buttie.catalog.domain.PolicyVO">
    select *
    from POLICY
    where POLICY_ID = #{policyId}
  </select>

  <select id="findAll" resultType="com.talented.buttie.catalog.domain.PolicyVO">
    select *
    from POLICY
    where POLICY_STATUS = 'AVAILABLE'
    order by case when EXTERNAL_SOURCE = 'YOUTHCENTER' then 0 else 1 end asc, POLICY_ID asc
  </select>

  <select id="findAllPaginated" resultType="com.talented.buttie.catalog.domain.PolicyVO">
    select *
    from POLICY
    where POLICY_STATUS = 'AVAILABLE'
    order by case when EXTERNAL_SOURCE = 'YOUTHCENTER' then 0 else 1 end asc, POLICY_ID asc
    limit #{pageSize} offset #{offset}
  </select>

  <select id="countAll" resultType="long">
    select count(*)
    from POLICY
    where POLICY_STATUS = 'AVAILABLE'
  </select>

  <!-- ========================================================= -->
  <!-- 2. 사용자 조건 검색 WHERE 조항                              -->
  <!-- ========================================================= -->
  <sql id="searchWhereClause">
    <where>
      <choose>
        <when test="request.policyStatus != null">
          p.POLICY_STATUS = #{request.policyStatus}
        </when>
        <otherwise>
          p.POLICY_STATUS = 'AVAILABLE'
        </otherwise>
      </choose>

      <!-- 키워드 검색 -->
      <if test="request.keyword != null and request.keyword != ''">
        and (p.POLICY_NAME like concat('%', #{request.keyword}, '%')
        or p.REQUIRED_DOCUMENT like concat('%', #{request.keyword}, '%'))
      </if>

      <!-- 카테고리 필터 (policyCategory 단일 파라미터 사용, OGNL 안전 동등 비교) -->
      <if test="request.policyCategory != null">
        <choose>
          <when
            test="request.policyCategory == @com.talented.buttie.catalog.domain.PolicyCategory@HOUSING">
            and (p.POLICY_CATEGORY like '%주거%' or p.POLICY_CATEGORY like '%월세%' or p.POLICY_CATEGORY
            like '%전세%' or p.POLICY_CATEGORY like '%임대%')
          </when>
          <when
            test="request.policyCategory == @com.talented.buttie.catalog.domain.PolicyCategory@TRANSPORT">
            and (p.POLICY_CATEGORY like '%교통%' or p.POLICY_CATEGORY like '%패스%' or p.POLICY_CATEGORY
            like '%이동%')
          </when>
          <when
            test="request.policyCategory == @com.talented.buttie.catalog.domain.PolicyCategory@WELFARE">
            and (p.POLICY_CATEGORY like '%복지%' or p.POLICY_CATEGORY like '%생활%' or p.POLICY_CATEGORY
            like '%건강%' or p.POLICY_CATEGORY like '%의료%' or p.POLICY_CATEGORY like '%지원%')
          </when>
          <when
            test="request.policyCategory == @com.talented.buttie.catalog.domain.PolicyCategory@EMPLOYMENT">
            and (p.POLICY_CATEGORY like '%취업%' or p.POLICY_CATEGORY like '%일자리%' or
            p.POLICY_CATEGORY like '%구직%' or p.POLICY_CATEGORY like '%인턴%')
          </when>
          <when
            test="request.policyCategory == @com.talented.buttie.catalog.domain.PolicyCategory@EDUCATION">
            and (p.POLICY_CATEGORY like '%교육%' or p.POLICY_CATEGORY like '%역량강화%' or
            p.POLICY_CATEGORY like '%자격증%' or p.POLICY_CATEGORY like '%학자금%' or p.POLICY_CATEGORY
            like '%장학%')
          </when>
          <when
            test="request.policyCategory == @com.talented.buttie.catalog.domain.PolicyCategory@YOUTH_SUPPORT">
            and (p.POLICY_CATEGORY like '%청년%' or p.POLICY_CATEGORY like '%자산%' or p.POLICY_CATEGORY
            like '%참여%' or p.POLICY_CATEGORY like '%창업%')
          </when>
          <otherwise>
            and p.POLICY_CATEGORY like '%기타%'
          </otherwise>
        </choose>
      </if>

      <!-- 정책 지역 필터 ('전국' 검색 시 전체 지역 포함 조회, 특정 지자체 입력 시 해당 지자체 + 전국 공통 정책 조회) -->
      <if test="request.policyRegion != null and request.policyRegion != ''">
        <if
          test="request.policyRegion != '전국' and request.policyRegion != '전국공통' and request.policyRegion != '전체'">
          <bind name="shortRegion"
            value="request.policyRegion.replace('서울특별시','서울').replace('경기도','경기').replace('인천광역시','인천').replace('부산광역시','부산').replace('대구광역시','대구').replace('광주광역시','광주').replace('대전광역시','대전').replace('울산광역시','울산').replace('세종특별자치시','세종').replace('전라남도','전남').replace('전북특별자치도','전북').replace('경상남도','경남').replace('경상북도','경북').replace('충청남도','충남').replace('충청북도','충북').replace('강원특별자치도','강원').replace('제주특별자치도','제주')"/>
          and (
          p.POLICY_REGION = '전국'
          or p.POLICY_REGION like concat('%', #{request.policyRegion}, '%')
          or p.POLICY_REGION like concat('%', #{shortRegion}, '%')
          or p.POLICY_NAME like concat('%', #{shortRegion}, '%')
          or (m.REGION_NAME is not null and (m.REGION_NAME like concat('%', #{request.policyRegion},
          '%') or m.REGION_NAME like concat('%', #{shortRegion}, '%')))
          or (m.ZIP_CD is not null and m.ZIP_CD = #{request.policyRegion})
          )
        </if>
      </if>

      <!-- 사용자 연령 필터 -->
      <if test="request.age != null">
        and (p.POLICY_MIN_AGE is null or #{request.age} &gt;= p.POLICY_MIN_AGE)
        and (p.POLICY_MAX_AGE is null or #{request.age} &lt;= p.POLICY_MAX_AGE)
      </if>

      <!-- 취업 준비 상태 필터 (OGNL 예외를 방지하는 완전 동등 비교 및 쉼표 다중 값 토큰 포함 SQL 매핑) -->
      <if test="request.employmentPrepStatus != null and request.employmentPrepStatus != ''">
        <choose>
          <when
            test="request.employmentPrepStatus == 'EMPLOYED' or request.employmentPrepStatus == '재직자' or request.employmentPrepStatus == '재직'">
            and (p.EMPLOYMENT_PREP_STATUS like '%재직%' or p.EMPLOYMENT_PREP_STATUS like '%취업%' or
            p.EMPLOYMENT_PREP_STATUS like '%무관%' or p.EMPLOYMENT_PREP_STATUS like '%ALL%' or
            p.EMPLOYMENT_PREP_STATUS is null)
          </when>
          <when
            test="request.employmentPrepStatus == 'PROSPECTIVE_FOUNDER' or request.employmentPrepStatus == '예비창업자' or request.employmentPrepStatus == '창업자' or request.employmentPrepStatus == '창업'">
            and (p.EMPLOYMENT_PREP_STATUS like '%창업%' or p.EMPLOYMENT_PREP_STATUS like '%무관%' or
            p.EMPLOYMENT_PREP_STATUS like '%ALL%' or p.EMPLOYMENT_PREP_STATUS is null)
          </when>
          <when
            test="request.employmentPrepStatus == 'UNEMPLOYED' or request.employmentPrepStatus == '미취업자' or request.employmentPrepStatus == '미취업'">
            and (p.EMPLOYMENT_PREP_STATUS like '%미취업%' or p.EMPLOYMENT_PREP_STATUS like '%첫취업%' or
            p.EMPLOYMENT_PREP_STATUS like '%재취업%' or p.EMPLOYMENT_PREP_STATUS like '%무관%' or
            p.EMPLOYMENT_PREP_STATUS like '%ALL%' or p.EMPLOYMENT_PREP_STATUS is null)
          </when>
          <when
            test="request.employmentPrepStatus == 'FIRST_JOB' or request.employmentPrepStatus == '첫취업' or request.employmentPrepStatus == '첫'">
            and (p.EMPLOYMENT_PREP_STATUS like '%첫취업%' or p.EMPLOYMENT_PREP_STATUS like '%첫%' or
            p.EMPLOYMENT_PREP_STATUS like '%미취업%' or p.EMPLOYMENT_PREP_STATUS like '%무관%' or
            p.EMPLOYMENT_PREP_STATUS like '%ALL%' or p.EMPLOYMENT_PREP_STATUS is null)
          </when>
          <when
            test="request.employmentPrepStatus == 'REEMPLOYMENT' or request.employmentPrepStatus == '재취업'">
            and (p.EMPLOYMENT_PREP_STATUS like '%재취업%' or p.EMPLOYMENT_PREP_STATUS like '%미취업%' or
            p.EMPLOYMENT_PREP_STATUS like '%무관%' or p.EMPLOYMENT_PREP_STATUS like '%ALL%' or
            p.EMPLOYMENT_PREP_STATUS is null)
          </when>
          <otherwise>
            and (p.EMPLOYMENT_PREP_STATUS like concat('%', #{request.employmentPrepStatus}, '%') or
            p.EMPLOYMENT_PREP_STATUS like '%무관%' or p.EMPLOYMENT_PREP_STATUS like '%ALL%' or
            p.EMPLOYMENT_PREP_STATUS is null)
          </otherwise>
        </choose>
      </if>

      <!-- 지원 금액 필터 (policySupportAmount 요청 시 최소 금액 조건이 엄격히 검증된 정책만 반환) -->
      <if test="request.policySupportAmount != null">
        and p.POLICY_SUPPORT_AMOUNT &gt;= #{request.policySupportAmount}
      </if>

      <!-- 마감일 (정확한 마감일 또는 마감일 범위 필터, 하한을 current_date()로 설정해 오늘 마감 정책의 00:00:00 포함) -->
      <if test="request.dueDate != null">
        and (p.DUE_DATE is null or p.DUE_DATE &lt;= #{request.dueDate})
      </if>
      <if test="request.dueDateFilter != null and request.dueDateFilter != ''">
        <choose>
          <when test="request.dueDateFilter == 'ALWAYS' or request.dueDateFilter == '상시'">
            and p.DUE_DATE is null
          </when>
          <when
            test="request.dueDateFilter == 'TODAY' or request.dueDateFilter == '오늘' or request.dueDateFilter == '오늘 마감'">
            and p.DUE_DATE between current_date() and concat(current_date(), ' 23:59:59')
          </when>
          <when
            test="request.dueDateFilter == 'WITHIN_3_DAYS' or request.dueDateFilter == '3일' or request.dueDateFilter == '3일 이내'">
            and p.DUE_DATE between current_date() and concat(date_add(current_date(), interval 3
            day), ' 23:59:59')
          </when>
          <when
            test="request.dueDateFilter == 'WITHIN_7_DAYS' or request.dueDateFilter == '7일' or request.dueDateFilter == '7일 이내'">
            and p.DUE_DATE between current_date() and concat(date_add(current_date(), interval 7
            day), ' 23:59:59')
          </when>
          <when
            test="request.dueDateFilter == 'WITHIN_30_DAYS' or request.dueDateFilter == '30일' or request.dueDateFilter == '30일 이내'">
            and p.DUE_DATE between current_date() and concat(date_add(current_date(), interval 30
            day), ' 23:59:59')
          </when>
        </choose>
      </if>
    </where>
  </sql>

  <!-- ========================================================= -->
  <!-- 3. 조건 검색 (온통청년 API 수집 정책 최우선 정렬)             -->
  <!-- ========================================================= -->
  <select id="searchPolicies" resultType="com.talented.buttie.catalog.domain.PolicyVO">
    select distinct p.*
    from POLICY p
    left join POLICY_REGION_MAP m on m.POLICY_ID = p.POLICY_ID
    <include refid="searchWhereClause"/>
    order by case when p.EXTERNAL_SOURCE = 'YOUTHCENTER' then 0 else 1 end asc, p.POLICY_ID asc
    limit #{pageSize} offset #{offset}
  </select>

  <select id="countSearchPolicies" resultType="long">
    select count(distinct p.POLICY_ID)
    from POLICY p
    left join POLICY_REGION_MAP m on m.POLICY_ID = p.POLICY_ID
    <include refid="searchWhereClause"/>
  </select>

  <!-- ========================================================= -->
  <!-- 4. 어드민 관리자 & Batch CUD                               -->
  <!-- ========================================================= -->
  <select id="search" resultType="com.talented.buttie.catalog.domain.PolicyVO">
    select * from POLICY
    <where>
      <if test="externalSource != null and externalSource != ''">
        and EXTERNAL_SOURCE = #{externalSource}
      </if>
      <if test="amountParseConfidence != null and amountParseConfidence != ''">
        and AMOUNT_PARSE_CONFIDENCE = #{amountParseConfidence}
      </if>
    </where>
    order by POLICY_ID desc
    limit #{size} offset #{offset}
  </select>

  <select id="countBySearch" resultType="int">
    select count(*) from POLICY
    <where>
      <if test="externalSource != null and externalSource != ''">
        and EXTERNAL_SOURCE = #{externalSource}
      </if>
      <if test="amountParseConfidence != null and amountParseConfidence != ''">
        and AMOUNT_PARSE_CONFIDENCE = #{amountParseConfidence}
      </if>
    </where>
  </select>

  <insert id="insert" parameterType="com.talented.buttie.catalog.domain.PolicyVO"
    useGeneratedKeys="true" keyProperty="policyId">
    insert into POLICY (POLICY_NAME, POLICY_CATEGORY, POLICY_MIN_AGE, POLICY_MAX_AGE, POLICY_REGION,
                        POLICY_SUPPORT_AMOUNT, SUPPORT_MONTH_COUNT, DUE_DATE, REQUIRED_DOCUMENT,
                        EMPLOYMENT_PREP_STATUS, FAMILY_COUNT, POLICY_STATUS, POLICY_URL,
                        EXTERNAL_SOURCE, EXTERNAL_POLICY_ID, AMOUNT_PARSE_CONFIDENCE)
    values (#{policyName}, #{policyCategory}, #{policyMinAge}, #{policyMaxAge}, #{policyRegion},
            #{policySupportAmount}, #{supportMonthCount}, #{dueDate}, #{requiredDocument},
            #{employmentPrepStatus}, #{familyCount}, #{policyStatus}, #{policyUrl},
            #{externalSource}, #{externalPolicyId}, #{amountParseConfidence})
  </insert>

  <update id="update" parameterType="com.talented.buttie.catalog.domain.PolicyVO">
    update POLICY
    set POLICY_NAME = #{policyName},
    POLICY_CATEGORY = #{policyCategory},
    POLICY_MIN_AGE = #{policyMinAge},
    POLICY_MAX_AGE = #{policyMaxAge},
    POLICY_REGION = #{policyRegion},
    POLICY_SUPPORT_AMOUNT = #{policySupportAmount},
    SUPPORT_MONTH_COUNT = #{supportMonthCount},
    DUE_DATE = #{dueDate},
    REQUIRED_DOCUMENT = #{requiredDocument},
    EMPLOYMENT_PREP_STATUS = #{employmentPrepStatus},
    FAMILY_COUNT = #{familyCount},
    POLICY_STATUS = #{policyStatus},
    POLICY_URL = #{policyUrl}
    <if test="externalSource != null and externalSource != ''">
      , EXTERNAL_SOURCE = #{externalSource}
    </if>
    <if test="externalPolicyId != null and externalPolicyId != ''">
      , EXTERNAL_POLICY_ID = #{externalPolicyId}
    </if>
    <if test="amountParseConfidence != null">
      , AMOUNT_PARSE_CONFIDENCE = #{amountParseConfidence}
    </if>
    where POLICY_ID = #{policyId}
  </update>

  <delete id="deleteById">
    delete
    from POLICY
    where POLICY_ID = #{policyId}
  </delete>

  <insert id="upsert" parameterType="com.talented.buttie.catalog.domain.PolicyVO"
    useGeneratedKeys="true" keyProperty="policyId">
    insert into POLICY (POLICY_NAME, POLICY_CATEGORY, POLICY_MIN_AGE, POLICY_MAX_AGE, POLICY_REGION,
                        POLICY_SUPPORT_AMOUNT, SUPPORT_MONTH_COUNT, DUE_DATE, REQUIRED_DOCUMENT,
                        EMPLOYMENT_PREP_STATUS, FAMILY_COUNT, POLICY_STATUS, POLICY_URL,
                        EXTERNAL_SOURCE, EXTERNAL_POLICY_ID, AMOUNT_PARSE_CONFIDENCE)
    values (#{policyName}, #{policyCategory}, #{policyMinAge}, #{policyMaxAge}, #{policyRegion},
            #{policySupportAmount}, #{supportMonthCount}, #{dueDate}, #{requiredDocument},
            #{employmentPrepStatus}, #{familyCount}, #{policyStatus}, #{policyUrl},
            #{externalSource}, #{externalPolicyId}, #{amountParseConfidence})
    on duplicate key update POLICY_ID               = last_insert_id(POLICY_ID),
                            POLICY_NAME             = values(POLICY_NAME),
                            POLICY_CATEGORY         = values(POLICY_CATEGORY),
                            POLICY_MIN_AGE          = values(POLICY_MIN_AGE),
                            POLICY_MAX_AGE          = values(POLICY_MAX_AGE),
                            POLICY_REGION           = values(POLICY_REGION),
                            POLICY_SUPPORT_AMOUNT   = values(POLICY_SUPPORT_AMOUNT),
                            SUPPORT_MONTH_COUNT     = values(SUPPORT_MONTH_COUNT),
                            DUE_DATE                = values(DUE_DATE),
                            REQUIRED_DOCUMENT       = values(REQUIRED_DOCUMENT),
                            EMPLOYMENT_PREP_STATUS  = values(EMPLOYMENT_PREP_STATUS),
                            FAMILY_COUNT            = values(FAMILY_COUNT),
                            POLICY_STATUS           = values(POLICY_STATUS),
                            POLICY_URL              = values(POLICY_URL),
                            AMOUNT_PARSE_CONFIDENCE = values(AMOUNT_PARSE_CONFIDENCE)
  </insert>

</mapper>
