# MySQL connection settings. All values must be provided as environment variables.
jdbc.driver=${MYSQL_DRIVER}
jdbc.url=jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
jdbc.username=${MYSQL_USER}
jdbc.password=${MYSQL_PASSWORD}
